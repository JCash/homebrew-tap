# JCash Homebrew tap

Install `jc-voronoi` directly from this tap:

```sh
brew install JCash/tap/jc-voronoi
```

The formula is updated automatically after a stable release is published in
[`JCash/voronoi`](https://github.com/JCash/voronoi).
